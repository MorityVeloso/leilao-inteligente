# Monitoramento de Leilão Ao Vivo — Plano de Implementação

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Permitir que o usuário acompanhe um leilão de gado ao vivo no YouTube em tempo real, com extração automática dos dados de cada lote, comparações históricas multicamada, e taxa de arrematação por faixa de preço — tudo dentro do dashboard React.

**Architecture:** O backend captura 1 frame a cada 5 segundos do stream do YouTube (via ffmpeg + stream URL), envia ao Gemini (sem thinking, ~2s resposta), detecta troca de lote por mudança de `lote_numero`, e envia eventos SSE ao frontend. O frontend mostra o lote atual com análises comparativas atualizadas em tempo real conforme o preço muda. Sessões ao vivo são persistidas como leilões normais no banco.

**Tech Stack:** Python (FastAPI, yt-dlp, ffmpeg, google-genai), React 19 (TypeScript, TanStack Query, Recharts, SSE via EventSource), SQLAlchemy, Supabase PostgreSQL.

---

## Visão geral do fluxo

```
Usuário cola URL no módulo "Ao Vivo"
    │
    ▼
Backend: valida se é live (yt-dlp --no-download → is_live?)
    │
    ▼
Dashboard: mostra player YouTube embedado + botões
    Estado: CONECTADO (pausado, aguardando início do leilão)
    │
    ▼
Usuário clica "Iniciar Análise"
    │
    ▼
Backend: loop a cada 5s
    ├── ffmpeg extrai 1 frame do stream
    ├── Change detection (comparar com frame anterior)
    ├── Se mudou: Gemini extrai dados (sem thinking, ~2s)
    ├── Compara lote_numero: mesmo → atualiza preço; diferente → novo lote
    ├── Salva no banco (lotes parciais, atualizados incrementalmente)
    └── Envia SSE event ao frontend
    │
    ▼
Dashboard: recebe SSE events
    ├── Atualiza painel do lote atual
    ├── Recalcula taxa de arrematação para o preço atual
    ├── Atualiza comparações multicamada
    └── Adiciona ao histórico quando lote troca
    │
    ▼
Usuário clica "Encerrar"
    │
    ▼
Backend: consolida sessão como leilão normal → salva no banco
```

---

## Modelo de dados

### Novos conceitos

```python
# Sessão ao vivo (equivale a um "job" de processamento)
SessaoAoVivo:
    id: str (UUID)
    url: str
    canal: str
    titulo: str
    status: str  # "conectado", "analisando", "pausado", "encerrado"
    leilao_id: int | None  # FK para Leilao (criado quando encerra)
    criado_em: datetime
    iniciado_em: datetime | None  # quando clicou "Iniciar"
    encerrado_em: datetime | None

# Lote ao vivo (estado incremental, atualizado a cada frame)
LoteAoVivo:
    sessao_id: str (FK)
    lote_numero: str
    quantidade: int
    raca: str
    sexo: str
    condicao: str | None
    idade_meses: int | None
    fazenda_vendedor: str | None
    preco_atual: Decimal  # último preço observado
    preco_inicial: Decimal  # primeiro preço > 0
    preco_maximo: Decimal  # máximo observado
    frames_analisados: int
    confianca_media: float
    carimbo_vendido: bool
    status: str  # "em_curso", "arrematado", "incerto"
    inicio: datetime  # quando o lote apareceu
    fim: datetime | None  # quando trocou pro próximo
```

---

### Task 1: Backend — Modelo de dados e sessão ao vivo

**Objetivo:** Criar as estruturas de dados para sessão ao vivo e lotes incrementais. Não usar tabelas novas no banco — manter em memória (dict) durante a sessão e persistir como Leilao/Lote normal ao encerrar.

**Files:**
- Create: `src/leilao_inteligente/pipeline/ao_vivo.py`

**Step 1: Criar o módulo ao_vivo.py com dataclasses**

```python
"""Motor de monitoramento de leilão ao vivo."""

import logging
import time
import threading
from dataclasses import dataclass, field
from datetime import datetime, timezone
from decimal import Decimal
from pathlib import Path
from queue import Queue

logger = logging.getLogger(__name__)


@dataclass
class LoteAoVivo:
    """Estado incremental de um lote durante monitoramento ao vivo."""
    lote_numero: str
    quantidade: int = 0
    raca: str | None = None
    sexo: str | None = None
    condicao: str | None = None
    idade_meses: int | None = None
    fazenda_vendedor: str | None = None
    preco_atual: Decimal = Decimal("0")
    preco_inicial: Decimal = Decimal("0")
    preco_maximo: Decimal = Decimal("0")
    frames_analisados: int = 0
    confianca_media: float = 0.0
    carimbo_vendido: bool = False
    status: str = "em_curso"  # em_curso, arrematado, incerto
    inicio: datetime = field(default_factory=lambda: datetime.now(tz=timezone.utc))
    fim: datetime | None = None
    _precos: list[Decimal] = field(default_factory=list, repr=False)
    _confiancas: list[float] = field(default_factory=list, repr=False)

    def atualizar_com_frame(self, dados: dict) -> None:
        """Atualiza o lote com dados de um novo frame."""
        preco = Decimal(str(dados.get("preco_lance", 0)))

        if preco > 0:
            self._precos.append(preco)
            self.preco_atual = preco
            if self.preco_inicial == 0:
                self.preco_inicial = preco
            self.preco_maximo = max(self.preco_maximo, preco)

        # Atualizar campos com valor mais recente não-nulo
        for campo in ["quantidade", "raca", "sexo", "condicao", "idade_meses", "fazenda_vendedor"]:
            valor = dados.get(campo)
            if valor is not None:
                setattr(self, campo, valor)

        # Carimbo: uma vez true, sempre true
        if dados.get("carimbo_vendido"):
            self.carimbo_vendido = True
            self.status = "arrematado"

        conf = dados.get("confianca", 0.0)
        if conf:
            self._confiancas.append(float(conf))
            self.confianca_media = sum(self._confiancas) / len(self._confiancas)

        self.frames_analisados += 1

    def finalizar(self) -> None:
        """Chamado quando o lote sai da pista (trocou pro próximo)."""
        self.fim = datetime.now(tz=timezone.utc)
        if self.status == "em_curso":
            if self.carimbo_vendido:
                self.status = "arrematado"
            elif self.preco_atual > self.preco_inicial and self.preco_inicial > 0:
                self.status = "arrematado"
            elif len(set(self._precos)) >= 3:
                self.status = "arrematado"
            else:
                self.status = "incerto"

    def to_dict(self) -> dict:
        """Serializa para enviar via SSE."""
        return {
            "lote_numero": self.lote_numero,
            "quantidade": self.quantidade,
            "raca": self.raca,
            "sexo": self.sexo,
            "condicao": self.condicao,
            "idade_meses": self.idade_meses,
            "fazenda_vendedor": self.fazenda_vendedor,
            "preco_atual": float(self.preco_atual),
            "preco_inicial": float(self.preco_inicial),
            "preco_maximo": float(self.preco_maximo),
            "frames_analisados": self.frames_analisados,
            "confianca_media": round(self.confianca_media, 2),
            "carimbo_vendido": self.carimbo_vendido,
            "status": self.status,
            "inicio": self.inicio.isoformat(),
            "fim": self.fim.isoformat() if self.fim else None,
            "precos_historico": [float(p) for p in self._precos],
        }


@dataclass
class SessaoAoVivo:
    """Sessão de monitoramento ao vivo."""
    id: str
    url: str
    canal: str
    titulo: str
    status: str = "conectado"  # conectado, analisando, pausado, encerrado
    lote_atual: LoteAoVivo | None = None
    lotes_finalizados: list[LoteAoVivo] = field(default_factory=list)
    eventos: Queue = field(default_factory=Queue, repr=False)
    _thread: threading.Thread | None = field(default=None, repr=False)
    _parar: threading.Event = field(default_factory=threading.Event, repr=False)
    criado_em: datetime = field(default_factory=lambda: datetime.now(tz=timezone.utc))
    iniciado_em: datetime | None = None
    encerrado_em: datetime | None = None

    def total_lotes(self) -> int:
        n = len(self.lotes_finalizados)
        if self.lote_atual:
            n += 1
        return n

    def to_dict(self) -> dict:
        return {
            "id": self.id,
            "url": self.url,
            "canal": self.canal,
            "titulo": self.titulo,
            "status": self.status,
            "lote_atual": self.lote_atual.to_dict() if self.lote_atual else None,
            "lotes_finalizados": [l.to_dict() for l in self.lotes_finalizados],
            "total_lotes": self.total_lotes(),
            "criado_em": self.criado_em.isoformat(),
            "iniciado_em": self.iniciado_em.isoformat() if self.iniciado_em else None,
        }
```

**Step 2: Run tests**

```bash
uv run python -c "from leilao_inteligente.pipeline.ao_vivo import LoteAoVivo, SessaoAoVivo; print('OK')"
```

**Step 3: Commit**

```bash
git add src/leilao_inteligente/pipeline/ao_vivo.py
git commit -m "feat(ao-vivo): modelo de dados LoteAoVivo + SessaoAoVivo"
```

---

### Task 2: Backend — Motor de captura ao vivo

**Objetivo:** Implementar o loop de captura que extrai frames do stream, envia ao Gemini, detecta troca de lote, e emite eventos SSE.

**Files:**
- Modify: `src/leilao_inteligente/pipeline/ao_vivo.py`

**Step 1: Adicionar função de validação de live stream**

```python
def validar_live(url: str) -> dict:
    """Valida se a URL é um stream ao vivo do YouTube.

    Returns:
        dict com: is_live, canal, titulo, video_id, erro (se houver)
    """
    import yt_dlp
    from leilao_inteligente.config import get_settings

    settings = get_settings()
    ydl_opts = {"quiet": True, "no_warnings": True}
    if settings.cookies_path:
        ydl_opts["cookiefile"] = str(settings.cookies_path)

    try:
        with yt_dlp.YoutubeDL(ydl_opts) as ydl:
            info = ydl.extract_info(url, download=False)
            is_live = info.get("is_live", False)
            return {
                "is_live": is_live,
                "canal": info.get("channel", ""),
                "titulo": info.get("title", ""),
                "video_id": info.get("id", ""),
                "erro": None if is_live else "Vídeo não é uma transmissão ao vivo",
            }
    except Exception as e:
        return {"is_live": False, "canal": "", "titulo": "", "video_id": "", "erro": str(e)}
```

**Step 2: Adicionar função de captura de frame do stream**

```python
def _capturar_frame_stream(stream_url: str, output_path: Path) -> bool:
    """Captura 1 frame do stream ao vivo via ffmpeg."""
    import subprocess

    cmd = [
        "ffmpeg", "-y",
        "-i", stream_url,
        "-frames:v", "1",
        "-q:v", "2",
        str(output_path),
    ]
    try:
        result = subprocess.run(cmd, capture_output=True, timeout=10)
        return result.returncode == 0 and output_path.exists()
    except Exception:
        return False


def _obter_stream_url(url: str) -> str | None:
    """Obtém URL do stream via yt-dlp."""
    import yt_dlp
    from leilao_inteligente.config import get_settings

    settings = get_settings()
    ydl_opts = {
        "format": "134/133/160/best[height<=360]",
        "quiet": True,
        "no_warnings": True,
    }
    if settings.cookies_path:
        ydl_opts["cookiefile"] = str(settings.cookies_path)

    try:
        with yt_dlp.YoutubeDL(ydl_opts) as ydl:
            info = ydl.extract_info(url, download=False)
            return info.get("url")
    except Exception as e:
        logger.error("Erro obtendo stream URL: %s", e)
        return None
```

**Step 3: Implementar o loop principal de captura**

```python
def _loop_captura(sessao: SessaoAoVivo, prompt_calibrado: str | None = None) -> None:
    """Loop principal: captura frame → Gemini → atualiza lote → emite evento.

    Roda em thread separada. Para quando sessao._parar é setado.
    """
    import json
    import tempfile
    from leilao_inteligente.pipeline.vision import _preparar_frame, _chamar_gemini, _parse_response, _get_client, PROMPT_EXTRACAO
    from leilao_inteligente.pipeline.validator import normalizar_dados
    from google.genai.types import Part

    intervalo = 5  # segundos
    stream_url = _obter_stream_url(sessao.url)
    if not stream_url:
        sessao.status = "erro"
        sessao.eventos.put({"tipo": "erro", "mensagem": "Não foi possível obter stream URL"})
        return

    client = _get_client()
    prompt = prompt_calibrado or PROMPT_EXTRACAO
    frame_anterior_bytes: bytes | None = None
    frame_num = 0

    logger.info("Iniciando captura ao vivo: %s", sessao.titulo)

    while not sessao._parar.is_set():
        if sessao.status == "pausado":
            time.sleep(1)
            continue

        frame_num += 1
        frame_path = Path(tempfile.mktemp(suffix=".jpg"))

        try:
            # 1. Capturar frame
            if not _capturar_frame_stream(stream_url, frame_path):
                # Stream URL pode ter expirado, renovar
                logger.info("Frame falhou, renovando stream URL...")
                stream_url = _obter_stream_url(sessao.url)
                if not stream_url:
                    sessao.eventos.put({"tipo": "erro", "mensagem": "Stream perdido"})
                    break
                continue

            # 2. Preparar e detectar mudança
            overlay_bytes = _preparar_frame(frame_path)

            if frame_anterior_bytes is not None:
                # Change detection simples: comparar bytes
                import numpy as np
                import cv2
                img_atual = cv2.imdecode(np.frombuffer(overlay_bytes, np.uint8), cv2.IMREAD_GRAYSCALE)
                img_anterior = cv2.imdecode(np.frombuffer(frame_anterior_bytes, np.uint8), cv2.IMREAD_GRAYSCALE)
                if img_atual.shape == img_anterior.shape:
                    diff = np.mean(np.abs(img_atual.astype(float) - img_anterior.astype(float)))
                    if diff < 3.0:  # Menos de 3/255 de diferença média = frame igual
                        frame_anterior_bytes = overlay_bytes
                        time.sleep(intervalo)
                        continue

            frame_anterior_bytes = overlay_bytes

            # 3. Enviar ao Gemini
            image_part = Part.from_bytes(data=overlay_bytes, mime_type="image/jpeg")
            response = _chamar_gemini(client, image_part, prompt=prompt)

            if not response.text:
                time.sleep(intervalo)
                continue

            dados = _parse_response(response.text)
            if not dados:
                # Frame sem overlay (intervalo, propaganda)
                sessao.eventos.put({"tipo": "frame_sem_dados", "frame": frame_num})
                time.sleep(intervalo)
                continue

            # 4. Normalizar dados
            dados = normalizar_dados(dados)
            lote_numero = str(dados.get("lote_numero", ""))

            if not lote_numero or lote_numero == "0":
                time.sleep(intervalo)
                continue

            # 5. Detectar troca de lote
            if sessao.lote_atual and sessao.lote_atual.lote_numero != lote_numero:
                # Lote trocou! Finalizar o anterior
                sessao.lote_atual.finalizar()
                sessao.lotes_finalizados.append(sessao.lote_atual)
                sessao.eventos.put({
                    "tipo": "lote_finalizado",
                    "lote": sessao.lote_atual.to_dict(),
                })
                sessao.lote_atual = None

            # 6. Criar ou atualizar lote
            if sessao.lote_atual is None:
                sessao.lote_atual = LoteAoVivo(lote_numero=lote_numero)
                sessao.eventos.put({"tipo": "novo_lote", "lote_numero": lote_numero})

            sessao.lote_atual.atualizar_com_frame(dados)

            # 7. Emitir evento de atualização
            sessao.eventos.put({
                "tipo": "lote_atualizado",
                "lote": sessao.lote_atual.to_dict(),
                "frame": frame_num,
            })

        except Exception as e:
            logger.error("Erro no frame %d: %s", frame_num, e)
            sessao.eventos.put({"tipo": "erro", "mensagem": str(e)})

        finally:
            # Limpar frame temporário
            if frame_path.exists():
                frame_path.unlink()

        time.sleep(intervalo)

    # Finalizar último lote se houver
    if sessao.lote_atual:
        sessao.lote_atual.finalizar()
        sessao.lotes_finalizados.append(sessao.lote_atual)
        sessao.lote_atual = None

    sessao.encerrado_em = datetime.now(tz=timezone.utc)
    sessao.status = "encerrado"
    sessao.eventos.put({"tipo": "sessao_encerrada"})
    logger.info("Sessão ao vivo encerrada: %d lotes", len(sessao.lotes_finalizados))
```

**Step 4: Adicionar métodos de controle na SessaoAoVivo**

```python
# Adicionar a SessaoAoVivo:

    def iniciar(self, prompt_calibrado: str | None = None) -> None:
        """Inicia a captura ao vivo em thread separada."""
        self.status = "analisando"
        self.iniciado_em = datetime.now(tz=timezone.utc)
        self._thread = threading.Thread(
            target=_loop_captura,
            args=(self, prompt_calibrado),
            daemon=True,
        )
        self._thread.start()

    def pausar(self) -> None:
        self.status = "pausado"

    def retomar(self) -> None:
        self.status = "analisando"

    def encerrar(self) -> None:
        self._parar.set()
        self.status = "encerrando"
```

**Step 5: Commit**

```bash
git add src/leilao_inteligente/pipeline/ao_vivo.py
git commit -m "feat(ao-vivo): motor de captura com loop 5s + change detection + Gemini"
```

---

### Task 3: Backend — Endpoints da API ao vivo

**Objetivo:** Criar os endpoints REST + SSE para o frontend interagir com a sessão ao vivo.

**Files:**
- Modify: `src/leilao_inteligente/api.py`

**Endpoints a criar:**

```
POST   /api/ao-vivo/conectar     → valida URL, cria sessão, retorna info
POST   /api/ao-vivo/iniciar      → inicia captura (botão "Iniciar Análise")
POST   /api/ao-vivo/pausar       → pausa captura (intervalo do leilão)
POST   /api/ao-vivo/retomar      → retoma captura
POST   /api/ao-vivo/encerrar     → encerra e persiste como leilão
GET    /api/ao-vivo/status       → estado atual da sessão + lote atual
GET    /api/ao-vivo/eventos      → SSE stream de eventos em tempo real
GET    /api/ao-vivo/historico    → lotes finalizados da sessão
GET    /api/ao-vivo/comparacao   → comparações históricas para o lote atual
```

**Step 1: Adicionar imports e estado global**

```python
# No topo de api.py, após os imports existentes:
from leilao_inteligente.pipeline.ao_vivo import SessaoAoVivo, validar_live

# Estado global (uma sessão ao vivo por vez)
_sessao_ao_vivo: SessaoAoVivo | None = None
```

**Step 2: Endpoint POST /api/ao-vivo/conectar**

```python
class ConectarAoVivoRequest(BaseModel):
    url: str

@app.post("/api/ao-vivo/conectar")
def post_ao_vivo_conectar(req: ConectarAoVivoRequest):
    """Valida URL e cria sessão ao vivo (estado CONECTADO)."""
    global _sessao_ao_vivo
    import uuid

    # Validar se é live
    info = validar_live(req.url)
    if not info["is_live"]:
        return JSONResponse({"erro": info["erro"]}, status_code=400)

    # Buscar calibração
    canal = info["canal"]

    _sessao_ao_vivo = SessaoAoVivo(
        id=uuid.uuid4().hex[:12],
        url=req.url,
        canal=canal,
        titulo=info["titulo"],
    )

    return {
        "sessao_id": _sessao_ao_vivo.id,
        "canal": canal,
        "titulo": info["titulo"],
        "video_id": info["video_id"],
        "status": "conectado",
        "tem_calibracao": obter_calibracao(canal) is not None,
    }
```

**Step 3: Endpoints de controle (iniciar/pausar/retomar/encerrar)**

```python
@app.post("/api/ao-vivo/iniciar")
def post_ao_vivo_iniciar():
    """Inicia captura ao vivo (botão 'Iniciar Análise')."""
    global _sessao_ao_vivo
    if not _sessao_ao_vivo:
        return JSONResponse({"erro": "Nenhuma sessão conectada"}, status_code=400)

    # Buscar prompt calibrado
    from leilao_inteligente.pipeline.calibration import obter_calibracao, montar_prompt_gemini
    cal = obter_calibracao(_sessao_ao_vivo.canal)
    prompt = montar_prompt_gemini(cal) if cal else None

    _sessao_ao_vivo.iniciar(prompt_calibrado=prompt)
    return {"status": "analisando"}


@app.post("/api/ao-vivo/pausar")
def post_ao_vivo_pausar():
    if not _sessao_ao_vivo:
        return JSONResponse({"erro": "Nenhuma sessão"}, status_code=400)
    _sessao_ao_vivo.pausar()
    return {"status": "pausado"}


@app.post("/api/ao-vivo/retomar")
def post_ao_vivo_retomar():
    if not _sessao_ao_vivo:
        return JSONResponse({"erro": "Nenhuma sessão"}, status_code=400)
    _sessao_ao_vivo.retomar()
    return {"status": "analisando"}


@app.post("/api/ao-vivo/encerrar")
def post_ao_vivo_encerrar():
    """Encerra sessão e persiste como leilão no banco."""
    global _sessao_ao_vivo
    if not _sessao_ao_vivo:
        return JSONResponse({"erro": "Nenhuma sessão"}, status_code=400)

    _sessao_ao_vivo.encerrar()

    # Aguardar thread finalizar (max 10s)
    if _sessao_ao_vivo._thread:
        _sessao_ao_vivo._thread.join(timeout=10)

    # Persistir como leilão
    from leilao_inteligente.storage.repository import salvar_leilao, _normalizar_titulo
    from leilao_inteligente.models.schemas import LeilaoInfo, LoteConsolidado
    from leilao_inteligente.pipeline.downloader import extrair_data_leilao, extrair_local_leilao

    lotes_consolidados = []
    todos_lotes = _sessao_ao_vivo.lotes_finalizados
    for lote in todos_lotes:
        lc = LoteConsolidado(
            lote_numero=lote.lote_numero,
            quantidade=lote.quantidade,
            raca=lote.raca,
            sexo=lote.sexo,
            condicao=lote.condicao,
            idade_meses=lote.idade_meses,
            preco_inicial=lote.preco_inicial,
            preco_final=lote.preco_atual,
            preco_por_cabeca=lote.preco_atual / lote.quantidade if lote.quantidade else None,
            fazenda_vendedor=lote.fazenda_vendedor,
            local_cidade=None,  # preenchido pelo LeilaoInfo
            local_estado=None,
            timestamp_inicio=lote.inicio,
            timestamp_fim=lote.fim,
            frames_analisados=lote.frames_analisados,
            confianca_media=lote.confianca_media,
            aparicoes=1,
            status=lote.status,
            frame_paths=[],
            segundo_video=None,
        )
        lotes_consolidados.append(lc)

    if lotes_consolidados:
        leilao_info = LeilaoInfo(
            canal_youtube=_sessao_ao_vivo.canal,
            url_video=_sessao_ao_vivo.url,
            titulo=_sessao_ao_vivo.titulo,
            data_leilao=datetime.now(tz=timezone.utc),
        )
        leilao = salvar_leilao(leilao_info, lotes_consolidados)
        resultado = {"status": "encerrado", "leilao_id": leilao.id, "lotes": len(lotes_consolidados)}
    else:
        resultado = {"status": "encerrado", "leilao_id": None, "lotes": 0}

    _sessao_ao_vivo = None
    return resultado
```

**Step 4: Endpoint GET /api/ao-vivo/status**

```python
@app.get("/api/ao-vivo/status")
def get_ao_vivo_status():
    """Retorna estado atual da sessão ao vivo."""
    if not _sessao_ao_vivo:
        return {"ativo": False}
    return {
        "ativo": True,
        **_sessao_ao_vivo.to_dict(),
    }
```

**Step 5: Endpoint GET /api/ao-vivo/eventos (SSE)**

```python
from fastapi.responses import StreamingResponse

@app.get("/api/ao-vivo/eventos")
def get_ao_vivo_eventos():
    """Stream SSE de eventos ao vivo."""
    if not _sessao_ao_vivo:
        return JSONResponse({"erro": "Nenhuma sessão"}, status_code=400)

    import json

    def event_stream():
        while _sessao_ao_vivo and _sessao_ao_vivo.status != "encerrado":
            try:
                evento = _sessao_ao_vivo.eventos.get(timeout=5)
                yield f"data: {json.dumps(evento, default=str)}\n\n"
            except Exception:
                # Timeout — enviar heartbeat
                yield f"data: {json.dumps({'tipo': 'heartbeat'})}\n\n"

        yield f"data: {json.dumps({'tipo': 'fim'})}\n\n"

    return StreamingResponse(
        event_stream(),
        media_type="text/event-stream",
        headers={
            "Cache-Control": "no-cache",
            "Connection": "keep-alive",
            "X-Accel-Buffering": "no",
        },
    )
```

**Step 6: Commit**

```bash
git add src/leilao_inteligente/api.py
git commit -m "feat(ao-vivo): endpoints REST + SSE para sessão ao vivo"
```

---

### Task 4: Backend — Comparações históricas multicamada

**Objetivo:** Endpoint que retorna comparações do lote ao vivo com dados históricos, filtrados por perfil exato + camadas geográficas.

**Files:**
- Modify: `src/leilao_inteligente/api.py`

**Step 1: Endpoint GET /api/ao-vivo/comparacao**

```python
@app.get("/api/ao-vivo/comparacao")
def get_ao_vivo_comparacao(
    lote_numero: str | None = None,
    n_leiloes: int = 5,
    casas: str | None = None,       # "Casa A,Casa B" — vazio = todas
    cidades: str | None = None,     # "Faina,Itapuranga"
    estados: str | None = None,     # "GO,MT"
):
    """Comparações históricas para o lote atual ao vivo.

    Retorna comparação multicamada: mesma casa, entre casas, cidades, estados.
    Filtro fixo: raça exata, sexo exato, idade ±1 mês, status arrematado.
    """
    if not _sessao_ao_vivo or not _sessao_ao_vivo.lote_atual:
        return {"comparacoes": []}

    lote = _sessao_ao_vivo.lote_atual
    session = get_session()

    try:
        # Perfil do lote atual
        raca = lote.raca
        sexo = lote.sexo
        idade = lote.idade_meses
        preco_atual = float(lote.preco_atual)

        if not all([raca, sexo, idade]):
            return {"comparacoes": [], "motivo": "Dados insuficientes do lote atual"}

        # Query base: mesma raça, sexo, idade ±1, arrematados
        from sqlalchemy import and_, or_

        def _query_comparacao(filtros_extra=None, label=""):
            q = session.query(Lote).join(Leilao).filter(
                Lote.raca == raca,
                Lote.sexo == sexo,
                Lote.idade_meses.between(idade - 1, idade + 1),
                Lote.status == "arrematado",
                Lote.preco_final > 0,
            )
            if filtros_extra:
                for f in filtros_extra:
                    q = q.filter(f)

            # Limitar por N últimos leilões
            leilao_ids = [r[0] for r in session.query(Leilao.id).order_by(
                func.coalesce(Leilao.data_leilao, Leilao.processado_em).desc()
            ).limit(n_leiloes).all()]
            if leilao_ids:
                q = q.filter(Lote.leilao_id.in_(leilao_ids))

            lotes_hist = q.all()
            if not lotes_hist:
                return None

            precos = [float(l.preco_final) for l in lotes_hist]
            precos_sorted = sorted(precos)
            n = len(precos)
            media = sum(precos) / n
            minimo = min(precos)
            maximo = max(precos)

            # Posição percentual
            abaixo = sum(1 for p in precos if p < preco_atual)
            percentual = round(abaixo / n * 100)

            # Taxa de arrematação por faixa de preço (quartis dinâmicos)
            import numpy as np
            quartis = np.percentile(precos, [25, 50, 75]).tolist()
            faixas = [
                (minimo, quartis[0]),
                (quartis[0], quartis[1]),
                (quartis[1], quartis[2]),
                (quartis[2], maximo),
            ]

            # Para taxa de arrematação, precisamos também dos NÃO arrematados
            q_todos = session.query(Lote).join(Leilao).filter(
                Lote.raca == raca,
                Lote.sexo == sexo,
                Lote.idade_meses.between(idade - 1, idade + 1),
                Lote.preco_final > 0,
            )
            if filtros_extra:
                for f in filtros_extra:
                    q_todos = q_todos.filter(f)
            if leilao_ids:
                q_todos = q_todos.filter(Lote.leilao_id.in_(leilao_ids))
            todos_lotes = q_todos.all()

            taxa_faixas = []
            faixa_atual = None
            for i, (fmin, fmax) in enumerate(faixas):
                na_faixa = [l for l in todos_lotes if fmin <= float(l.preco_final) <= fmax]
                arrematados = sum(1 for l in na_faixa if l.status == "arrematado")
                total_faixa = len(na_faixa)
                taxa = round(arrematados / total_faixa * 100) if total_faixa > 0 else 0
                faixa_dict = {
                    "min": round(fmin),
                    "max": round(fmax),
                    "total": total_faixa,
                    "arrematados": arrematados,
                    "taxa": taxa,
                }
                taxa_faixas.append(faixa_dict)
                if fmin <= preco_atual <= fmax:
                    faixa_atual = i

            # Tendência (comparar média dos 2 leilões mais recentes vs 2 anteriores)
            tendencia = None
            if n >= 4:
                meio = n // 2
                media_recente = sum(precos_sorted[meio:]) / len(precos_sorted[meio:])
                media_antiga = sum(precos_sorted[:meio]) / len(precos_sorted[:meio])
                if media_antiga > 0:
                    tendencia = round((media_recente - media_antiga) / media_antiga * 100, 1)

            return {
                "label": label,
                "media": round(media),
                "minimo": round(minimo),
                "maximo": round(maximo),
                "lotes": n,
                "percentual": percentual,
                "tendencia": tendencia,
                "taxa_faixas": taxa_faixas,
                "faixa_atual": faixa_atual,
                "n_leiloes_real": len(set(l.leilao_id for l in lotes_hist)),
            }

        comparacoes = []

        # Camada 1: Mesma casa de leilão
        casa_atual = _sessao_ao_vivo.titulo
        c = _query_comparacao(
            filtros_extra=[Leilao.titulo == casa_atual],
            label=f"Esta casa ({casa_atual[:30]})",
        )
        if c:
            comparacoes.append(c)

        # Camada 2: Casas específicas (se informadas)
        if casas:
            for casa in casas.split(","):
                casa = casa.strip()
                c = _query_comparacao(
                    filtros_extra=[Leilao.titulo == casa],
                    label=casa[:30],
                )
                if c:
                    comparacoes.append(c)

        # Camada 3: Cidades específicas (se informadas)
        if cidades:
            for cidade in cidades.split(","):
                cidade = cidade.strip()
                c = _query_comparacao(
                    filtros_extra=[Leilao.local_cidade == cidade],
                    label=f"Cidade: {cidade}",
                )
                if c:
                    comparacoes.append(c)

        # Camada 4: Estados específicos (se informados)
        if estados:
            for estado in estados.split(","):
                estado = estado.strip().upper()
                c = _query_comparacao(
                    filtros_extra=[Leilao.local_estado == estado],
                    label=f"Estado: {estado}",
                )
                if c:
                    comparacoes.append(c)

        # Camada 5: Todas as casas (geral)
        c = _query_comparacao(label="Todos os leilões")
        if c:
            comparacoes.append(c)

        return {
            "perfil": {
                "raca": raca,
                "sexo": sexo,
                "idade_meses": idade,
                "preco_atual": preco_atual,
            },
            "n_leiloes_solicitados": n_leiloes,
            "comparacoes": comparacoes,
        }

    finally:
        session.close()
```

**Step 2: Commit**

```bash
git add src/leilao_inteligente/api.py
git commit -m "feat(ao-vivo): endpoint de comparação histórica multicamada"
```

---

### Task 5: Frontend — API client e tipos para ao vivo

**Objetivo:** Adicionar tipos TypeScript e funções de API para o módulo ao vivo.

**Files:**
- Modify: `dashboard/src/lib/api.ts`

**Step 1: Adicionar interfaces**

```typescript
// Ao Vivo
export interface LoteAoVivo {
  lote_numero: string;
  quantidade: number;
  raca: string | null;
  sexo: string | null;
  condicao: string | null;
  idade_meses: number | null;
  fazenda_vendedor: string | null;
  preco_atual: number;
  preco_inicial: number;
  preco_maximo: number;
  frames_analisados: number;
  confianca_media: number;
  carimbo_vendido: boolean;
  status: string;
  inicio: string;
  fim: string | null;
  precos_historico: number[];
}

export interface SessaoAoVivo {
  ativo: boolean;
  id?: string;
  url?: string;
  canal?: string;
  titulo?: string;
  status?: string;
  lote_atual?: LoteAoVivo | null;
  lotes_finalizados?: LoteAoVivo[];
  total_lotes?: number;
  criado_em?: string;
  iniciado_em?: string;
}

export interface FaixaPreco {
  min: number;
  max: number;
  total: number;
  arrematados: number;
  taxa: number;
}

export interface ComparacaoCamada {
  label: string;
  media: number;
  minimo: number;
  maximo: number;
  lotes: number;
  percentual: number;
  tendencia: number | null;
  taxa_faixas: FaixaPreco[];
  faixa_atual: number | null;
  n_leiloes_real: number;
}

export interface ComparacaoResponse {
  perfil: {
    raca: string;
    sexo: string;
    idade_meses: number;
    preco_atual: number;
  };
  n_leiloes_solicitados: number;
  comparacoes: ComparacaoCamada[];
}

export interface EventoAoVivo {
  tipo: "lote_atualizado" | "lote_finalizado" | "novo_lote" | "frame_sem_dados" | "erro" | "heartbeat" | "sessao_encerrada" | "fim";
  lote?: LoteAoVivo;
  lote_numero?: string;
  mensagem?: string;
  frame?: number;
}
```

**Step 2: Adicionar funções de API**

```typescript
// Ao Vivo
aoVivoConectar: async (url: string) => {
  const resp = await fetch(`${API}/api/ao-vivo/conectar`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ url }),
  });
  return resp.json();
},

aoVivoIniciar: async () => {
  const resp = await fetch(`${API}/api/ao-vivo/iniciar`, { method: "POST" });
  return resp.json();
},

aoVivoPausar: async () => {
  const resp = await fetch(`${API}/api/ao-vivo/pausar`, { method: "POST" });
  return resp.json();
},

aoVivoRetomar: async () => {
  const resp = await fetch(`${API}/api/ao-vivo/retomar`, { method: "POST" });
  return resp.json();
},

aoVivoEncerrar: async () => {
  const resp = await fetch(`${API}/api/ao-vivo/encerrar`, { method: "POST" });
  return resp.json();
},

aoVivoStatus: async (): Promise<SessaoAoVivo> => {
  const resp = await fetch(`${API}/api/ao-vivo/status`);
  return resp.json();
},

aoVivoComparacao: async (params: {
  n_leiloes?: number;
  casas?: string;
  cidades?: string;
  estados?: string;
}): Promise<ComparacaoResponse> => {
  const p = new URLSearchParams();
  if (params.n_leiloes) p.set("n_leiloes", String(params.n_leiloes));
  if (params.casas) p.set("casas", params.casas);
  if (params.cidades) p.set("cidades", params.cidades);
  if (params.estados) p.set("estados", params.estados);
  const resp = await fetch(`${API}/api/ao-vivo/comparacao?${p}`);
  return resp.json();
},

aoVivoEventos: () => `${API}/api/ao-vivo/eventos`,  // URL para EventSource
```

**Step 3: Commit**

```bash
git add dashboard/src/lib/api.ts
git commit -m "feat(ao-vivo): tipos e API client para módulo ao vivo"
```

---

### Task 6: Frontend — Página Ao Vivo (layout e conexão)

**Objetivo:** Implementar a página principal do módulo ao vivo com player YouTube, input de URL, botões de controle, e conexão SSE.

**Files:**
- Rewrite: `dashboard/src/pages/ao-vivo.tsx`

**Layout definido com o usuário:**

```
┌──────────────────────────────────────────────────────────────────┐
│ Ao Vivo: [Título do Leilão]          [Pausar] [Encerrar]        │
├────────────────────────────────┬─────────────────────────────────┤
│                                │  LOTE 15                        │
│   [Player YouTube 16:9]       │  12x Bezerro Macho Nelore 10m   │
│                                │  R$ 3.100 (↑ de R$ 2.800)      │
│                                │  ⏱ 2m30s na pista              │
│                                │  Carimbo: ❌                    │
├────────────────────────────────┤                                 │
│  ┌─ TAXA DE ARREMATAÇÃO ─────┐│                                 │
│  │ ████████████ 88% ≤R$2.5k  ││                                 │
│  │ █████████░░░ 75% R$2.5-3k ││                                 │
│  │ ██████░░░░░░ 50% R$3-3.5k ← AQUI                            │
│  │ ██░░░░░░░░░░ 20% ≥R$3.5k  ││                                 │
│  └────────────────────────────┘│                                 │
├────────────────────────────────┼─────────────────────────────────┤
│  COMPARAÇÕES (multicamada)     │  HISTÓRICO DA SESSÃO            │
│  Esta casa: R$2.950 | >72%    │  14: 6x Garrote R$3.400 ✓      │
│  Faina-GO:  R$2.900 | >76%    │  13: 3x Vaca R$2.800 ?         │
│  Goiás:     R$2.860 | >79%    │  12: 25x Bezerro R$3.200 ✓     │
│  Todos:     R$2.824 | >81%    │  11: 10x Bezerra R$2.600 ✓     │
└────────────────────────────────┴─────────────────────────────────┘
```

**Hierarquia visual:**
1. Taxa de arrematação (maior destaque)
2. Comparações multicamada
3. Preço + dados do lote
4. Vídeo (player médio)
5. Histórico

Este é o componente mais complexo. Será implementado em etapas:

**Step 1: Estrutura base com estados e conexão URL**

Implementar a página com:
- Input para colar URL do YouTube
- Botão "Conectar" → chama `api.aoVivoConectar(url)`
- Se válido: mostra player YouTube embedado + botão "Iniciar Análise"
- Se inválido: mostra erro
- Botões: Iniciar / Pausar / Retomar / Encerrar
- Conexão SSE via `EventSource` quando análise inicia

**Step 2: Painel do lote atual**

Componente que recebe `LoteAoVivo` e mostra:
- Número, quantidade, raça, sexo, idade
- Preço atual com seta (subiu/desceu em relação ao inicial)
- Tempo na pista (calculado do `inicio`)
- Indicador de carimbo

**Step 3: Barra de taxa de arrematação**

Componente que recebe `FaixaPreco[]` e `faixa_atual`:
- 4 barras horizontais (quartis dinâmicos)
- Preenchimento proporcional à taxa de arrematação
- Destaque na faixa onde o preço atual se encontra
- Cores: verde (>75%), amarelo (50-75%), vermelho (<50%)

**Step 4: Comparações multicamada**

Componente que recebe `ComparacaoCamada[]`:
- Lista de camadas com label, média, min, max, lotes, percentual
- Indicador de tendência (seta ↗ ↘ →)
- Configuração: seletor de casas, cidades, estados, N leilões

**Step 5: Histórico da sessão**

Lista de lotes finalizados com:
- Número, tipo resumido, preço final, status (✓ arrematado, ? incerto)
- Métricas acumuladas: total lotes, arrematados, média

**Step 6: Commit**

```bash
git add dashboard/src/pages/ao-vivo.tsx
git commit -m "feat(ao-vivo): página completa com player, análise, comparações e histórico"
```

---

### Task 7: Frontend — Componentes auxiliares

**Objetivo:** Extrair componentes reutilizáveis da página ao vivo.

**Files:**
- Create: `dashboard/src/components/taxa-arrematacao.tsx`
- Create: `dashboard/src/components/comparacao-camadas.tsx`
- Create: `dashboard/src/components/lote-ao-vivo.tsx`

Cada componente recebe props tipadas e é independente. Detalhamento será feito durante a implementação baseado no código da Task 6.

**Step 1: Commit**

```bash
git add dashboard/src/components/taxa-arrematacao.tsx dashboard/src/components/comparacao-camadas.tsx dashboard/src/components/lote-ao-vivo.tsx
git commit -m "feat(ao-vivo): componentes taxa-arrematacao, comparacao-camadas, lote-ao-vivo"
```

---

### Task 8: Integração e teste end-to-end

**Objetivo:** Testar o fluxo completo com um stream real do YouTube.

**Files:**
- Nenhum novo — teste manual

**Step 1: Iniciar backend e frontend**

```bash
uv run uvicorn leilao_inteligente.api:app --reload --port 8000
cd dashboard && npm run dev
```

**Step 2: Fluxo de teste**

1. Abrir `localhost:5173/ao-vivo`
2. Colar URL de um leilão ao vivo (ou um vídeo gravado para simular — ajustar `validar_live` para aceitar gravado em modo dev)
3. Clicar "Conectar" → verificar player aparece
4. Clicar "Iniciar Análise" → verificar frames sendo processados
5. Observar lote atual atualizando preço
6. Esperar troca de lote → verificar lote anterior vai pro histórico
7. Verificar comparações atualizando
8. Clicar "Pausar" → verificar que para de capturar
9. Clicar "Retomar" → verificar que retoma
10. Clicar "Encerrar" → verificar leilão salvo no banco

**Step 3: Verificar dados**

```bash
python3 -c "
from leilao_inteligente.storage.db import get_session, init_db
from leilao_inteligente.models.database import Leilao
init_db()
s = get_session()
l = s.query(Leilao).order_by(Leilao.id.desc()).first()
print(f'Último leilão: {l.titulo} — {l.total_lotes} lotes')
s.close()
"
```

**Step 4: Commit final**

```bash
git add -A
git commit -m "feat(ao-vivo): módulo completo de monitoramento ao vivo com comparações históricas"
```

---

## Estimativa de custo ao vivo

```
Leilão de 4h ao vivo:
  Frames capturados: 4h × 720 frames/h = 2.880 frames
  Frames após change detection (~50%): 1.440 frames
  Chamadas Gemini (sem thinking): 1.440
  Input: 1.440 × (258 + 1.933) = 3.155.040 tokens
  Output: 1.440 × 200 = 288.000 tokens
  Custo Vertex AI: $0.47 + $0.17 = $0.64 = ~R$ 3,65

  Comparações: queries SQL, custo zero
```

---

## Resumo das Tasks

| Task | O que | Arquivos | Complexidade |
|------|-------|----------|-------------|
| 1 | Modelo de dados (LoteAoVivo, SessaoAoVivo) | ao_vivo.py (criar) | Baixa |
| 2 | Motor de captura (loop 5s, Gemini, change detection) | ao_vivo.py | Alta |
| 3 | Endpoints API (REST + SSE) | api.py | Média |
| 4 | Comparações históricas multicamada | api.py | Alta |
| 5 | API client TypeScript | api.ts | Baixa |
| 6 | Página ao vivo (layout completo) | ao-vivo.tsx | Alta |
| 7 | Componentes auxiliares | 3 componentes | Média |
| 8 | Teste end-to-end | Nenhum | Baixa |
