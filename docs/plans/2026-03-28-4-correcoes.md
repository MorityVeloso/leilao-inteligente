# 4 Correções do Leilão Inteligente — Plano de Implementação

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Corrigir path traversal na API, integrar segundo_video no pipeline, migrar dashboard para React Query, e expandir testes do processor.py.

**Architecture:** 4 tarefas independentes. Ordem: path traversal (2 linhas) → segundo_video (10 linhas) → testes processor (puro TDD) → React Query (refatoração 6 componentes). Cada tarefa tem commit isolado.

**Tech Stack:** Python (FastAPI, SQLAlchemy, Pydantic), React 19 (TanStack Query v5, Vite), Vitest/Pytest.

---

## Task 1: Fix path traversal no `/api/frame`

**Files:**
- Modify: `src/leilao_inteligente/api.py:460-466`
- Create: `tests/test_api_frame.py`

**Step 1: Write the failing test**

```python
# tests/test_api_frame.py
"""Testes para o endpoint /api/frame (path traversal)."""

from pathlib import Path
from unittest.mock import patch

import pytest
from fastapi.testclient import TestClient

from leilao_inteligente.api import app


client = TestClient(app)


class TestGetFrame:
    def test_rejeita_path_traversal(self):
        """Path com .. deve retornar 403."""
        response = client.get("/api/frame/../../.env")
        assert response.status_code == 403

    def test_rejeita_path_traversal_encoded(self):
        """Path traversal com encoding deve retornar 403."""
        response = client.get("/api/frame/..%2F..%2F.env")
        assert response.status_code == 403

    def test_frame_nao_encontrado(self):
        """Frame inexistente retorna 404."""
        response = client.get("/api/frame/lote_frames/inexistente/visual_1.jpg")
        assert response.status_code == 404

    def test_frame_valido(self, tmp_path):
        """Frame existente dentro de DATA_DIR retorna 200."""
        fake_frame = tmp_path / "test.jpg"
        fake_frame.write_bytes(b"\xff\xd8\xff\xe0")  # JPEG header

        with patch("leilao_inteligente.api.DATA_DIR", tmp_path):
            response = client.get("/api/frame/test.jpg")
            assert response.status_code == 200
```

**Step 2: Run test to verify it fails**

Run: `cd /Users/mority/Dev/Projects/leilao-inteligente && python -m pytest tests/test_api_frame.py -v`
Expected: FAIL — path traversal retorna 200 em vez de 403.

**Step 3: Implement the fix**

Em `src/leilao_inteligente/api.py`, substituir o endpoint:

```python
@app.get("/api/frame/{path:path}")
def get_frame(path: str):
    """Serve um frame visual do gado."""
    file_path = (DATA_DIR / path).resolve()
    if not file_path.is_relative_to(DATA_DIR.resolve()):
        return JSONResponse({"error": "Acesso negado"}, status_code=403)
    if not file_path.exists():
        return JSONResponse({"error": "Frame nao encontrado"}, status_code=404)
    return FileResponse(file_path)
```

Adicionar import de `JSONResponse` no topo:
```python
from fastapi.responses import FileResponse, JSONResponse
```

**Step 4: Run test to verify it passes**

Run: `python -m pytest tests/test_api_frame.py -v`
Expected: 4 PASSED

**Step 5: Commit**

```bash
git add tests/test_api_frame.py src/leilao_inteligente/api.py
git commit -m "fix: path traversal no endpoint /api/frame"
```

---

## Task 2: Integrar `segundo_video` no pipeline

**Files:**
- Modify: `src/leilao_inteligente/models/schemas.py:42-65` (LoteConsolidado)
- Modify: `src/leilao_inteligente/pipeline/processor.py:85-176` (consolidar_lotes)
- Modify: `src/leilao_inteligente/storage/repository.py:49-71` (salvar_leilao)
- Modify: `src/leilao_inteligente/api.py:180-192` (deletar dead code)
- Create: `tests/test_segundo_video.py`

**Step 1: Write the failing test**

```python
# tests/test_segundo_video.py
"""Testes para calculo automatico de segundo_video."""

from datetime import datetime, timezone
from decimal import Decimal
from pathlib import Path

from leilao_inteligente.models.schemas import LoteConsolidado


class TestSegundoVideoNoConsolidado:
    def test_campo_existe_no_schema(self):
        """LoteConsolidado deve ter campo segundo_video."""
        lote = LoteConsolidado(
            lote_numero="0001",
            quantidade=10,
            raca="Nelore",
            sexo="macho",
            preco_inicial=Decimal("1000"),
            preco_final=Decimal("2000"),
            local_cidade="Crixas",
            local_estado="GO",
            timestamp_inicio=datetime(2026, 3, 26, tzinfo=timezone.utc),
            frames_analisados=3,
            confianca_media=0.9,
            segundo_video=1610,
        )
        assert lote.segundo_video == 1610

    def test_segundo_video_default_none(self):
        """segundo_video deve ser None por padrao."""
        lote = LoteConsolidado(
            lote_numero="0001",
            quantidade=10,
            raca="Nelore",
            sexo="macho",
            preco_inicial=Decimal("1000"),
            preco_final=Decimal("2000"),
            local_cidade="Crixas",
            local_estado="GO",
            timestamp_inicio=datetime(2026, 3, 26, tzinfo=timezone.utc),
            frames_analisados=3,
            confianca_media=0.9,
        )
        assert lote.segundo_video is None
```

**Step 2: Run test to verify it fails**

Run: `python -m pytest tests/test_segundo_video.py -v`
Expected: FAIL — `segundo_video` is not a valid field.

**Step 3: Add campo ao schema**

Em `src/leilao_inteligente/models/schemas.py`, na classe `LoteConsolidado`, adicionar:

```python
    segundo_video: int | None = Field(default=None, description="Segundo do video YouTube onde o lote aparece")
```

**Step 4: Run test to verify it passes**

Run: `python -m pytest tests/test_segundo_video.py -v`
Expected: 2 PASSED

**Step 5: Write test para consolidar_lotes calcular segundo_video**

Adicionar ao `tests/test_segundo_video.py`:

```python
from leilao_inteligente.pipeline.processor import consolidar_lotes, LoteComFrame
from leilao_inteligente.models.schemas import LoteExtraido


def _make_lote_extraido(lote_numero: str, preco: float, ts_seconds: int) -> LoteExtraido:
    """Helper para criar LoteExtraido de teste."""
    base = datetime(2026, 3, 26, 20, 0, 0, tzinfo=timezone.utc)
    from datetime import timedelta
    return LoteExtraido(
        lote_numero=lote_numero,
        quantidade=10,
        raca="Nelore",
        sexo="macho",
        preco_lance=Decimal(str(preco)),
        local_cidade="Crixas",
        local_estado="GO",
        timestamp_frame=base + timedelta(seconds=ts_seconds),
        confianca=0.95,
    )


class TestConsolidarLotesSegundoVideo:
    def test_segundo_video_calculado_do_frame(self, tmp_path):
        """consolidar_lotes deve popular segundo_video a partir do nome do frame."""
        # frame_000323.jpg → (323-1)*5 = 1610s
        frame = tmp_path / "frame_000323.jpg"
        frame.write_bytes(b"\xff\xd8")

        lote = _make_lote_extraido("0001", 3000, 1610)
        lcf = LoteComFrame(lote, frame)

        resultado = consolidar_lotes([lcf])
        assert resultado[0].segundo_video == 1610

    def test_segundo_video_usa_primeiro_frame(self, tmp_path):
        """Deve usar o primeiro frame (menor timestamp) pro segundo_video."""
        frame1 = tmp_path / "frame_000100.jpg"
        frame2 = tmp_path / "frame_000200.jpg"
        frame1.write_bytes(b"\xff\xd8")
        frame2.write_bytes(b"\xff\xd8")

        lote1 = _make_lote_extraido("0001", 1000, 495)
        lote2 = _make_lote_extraido("0001", 2000, 995)

        resultado = consolidar_lotes([
            LoteComFrame(lote1, frame1),
            LoteComFrame(lote2, frame2),
        ])
        # Primeiro frame: (100-1)*5 = 495
        assert resultado[0].segundo_video == 495
```

**Step 6: Run test to verify it fails**

Run: `python -m pytest tests/test_segundo_video.py::TestConsolidarLotesSegundoVideo -v`
Expected: FAIL — `segundo_video` is None no resultado.

**Step 7: Implement calculo no consolidar_lotes**

Em `src/leilao_inteligente/pipeline/processor.py`, na funcao `consolidar_lotes`, adicionar import e logica:

No topo da funcao, adicionar import:
```python
from leilao_inteligente.pipeline.frame_extractor import frame_timestamp
```
(Ja existe no import do modulo, verificar.)

Dentro do loop `for numero, frames_lote in sorted(por_lote.items()):`, apos calcular `frame_paths_salvos`, antes de criar o `LoteConsolidado`:

```python
        # Calcular segundo_video a partir do primeiro frame
        segundo_video_val: int | None = None
        primeiro_frame_lcf = frames_com_preco[0]
        try:
            segundo_video_val = int(frame_timestamp(primeiro_frame_lcf.frame_path, 5))
        except (ValueError, IndexError):
            pass
```

E no construtor do `LoteConsolidado`, adicionar:
```python
            segundo_video=segundo_video_val,
```

**Step 8: Run test to verify it passes**

Run: `python -m pytest tests/test_segundo_video.py -v`
Expected: 4 PASSED

**Step 9: Salvar segundo_video no repository**

Em `src/leilao_inteligente/storage/repository.py`, no loop dentro de `salvar_leilao`, adicionar ao construtor do `Lote`:

```python
                segundo_video=lote_data.segundo_video,
```

(Adicionar na linha logo apos `frame_paths=...`)

**Step 10: Deletar dead code**

Em `src/leilao_inteligente/api.py`, deletar a funcao `_calcular_segundo_video` inteira (linhas 180-192).

**Step 11: Run all tests**

Run: `python -m pytest -v`
Expected: All PASSED

**Step 12: Commit**

```bash
git add src/leilao_inteligente/models/schemas.py src/leilao_inteligente/pipeline/processor.py src/leilao_inteligente/storage/repository.py src/leilao_inteligente/api.py tests/test_segundo_video.py
git commit -m "feat: segundo_video calculado automaticamente no pipeline"
```

---

## Task 3: Testes do `processor.py`

**Files:**
- Create: `tests/test_processor.py`

**Step 1: Write tests para _contar_aparicoes**

```python
# tests/test_processor.py
"""Testes para logica de consolidacao do processor."""

from datetime import datetime, timedelta, timezone
from decimal import Decimal
from pathlib import Path

from leilao_inteligente.models.schemas import LoteConsolidado, LoteExtraido
from leilao_inteligente.pipeline.processor import (
    LoteComFrame,
    _contar_aparicoes,
    _dedup_lotes_espelhados,
    _pegar_ultima_aparicao_lcf,
    consolidar_lotes,
    selecionar_frames_visuais,
)


BASE_TS = datetime(2026, 3, 26, 20, 0, 0, tzinfo=timezone.utc)


def _lote(
    numero: str = "0001",
    preco: float = 3000,
    offset_min: float = 0,
    qtd: int = 10,
    raca: str = "Nelore",
    sexo: str = "macho",
    confianca: float = 0.95,
) -> LoteExtraido:
    """Factory de LoteExtraido pra testes."""
    return LoteExtraido(
        lote_numero=numero,
        quantidade=qtd,
        raca=raca,
        sexo=sexo,
        preco_lance=Decimal(str(preco)),
        local_cidade="Crixas",
        local_estado="GO",
        timestamp_frame=BASE_TS + timedelta(minutes=offset_min),
        confianca=confianca,
    )


def _lcf(
    numero: str = "0001",
    preco: float = 3000,
    offset_min: float = 0,
    frame_name: str = "frame_000001.jpg",
    **kwargs,
) -> LoteComFrame:
    """Factory de LoteComFrame pra testes."""
    lote = _lote(numero=numero, preco=preco, offset_min=offset_min, **kwargs)
    return LoteComFrame(lote, Path(f"/tmp/{frame_name}"))


# --- _contar_aparicoes ---

class TestContarAparicoes:
    def test_um_frame(self):
        assert _contar_aparicoes([_lote()]) == 1

    def test_frames_proximos_uma_aparicao(self):
        """Frames com gap < 10 min = 1 aparicao."""
        frames = [_lote(offset_min=0), _lote(offset_min=5), _lote(offset_min=9)]
        assert _contar_aparicoes(frames) == 1

    def test_gap_grande_duas_aparicoes(self):
        """Gap > 10 min = repescagem = 2 aparicoes."""
        frames = [_lote(offset_min=0), _lote(offset_min=5), _lote(offset_min=20)]
        assert _contar_aparicoes(frames) == 2

    def test_tres_aparicoes(self):
        """Dois gaps grandes = 3 aparicoes."""
        frames = [
            _lote(offset_min=0),
            _lote(offset_min=5),
            _lote(offset_min=20),
            _lote(offset_min=25),
            _lote(offset_min=40),
        ]
        assert _contar_aparicoes(frames) == 3

    def test_gap_exato_10min_nao_e_repescagem(self):
        """Gap de exatamente 10 min nao conta como repescagem (precisa > 10)."""
        frames = [_lote(offset_min=0), _lote(offset_min=10)]
        assert _contar_aparicoes(frames) == 1


# --- _pegar_ultima_aparicao_lcf ---

class TestPegarUltimaAparicao:
    def test_sem_gap_retorna_tudo(self):
        frames = [_lcf(offset_min=0), _lcf(offset_min=5)]
        assert len(_pegar_ultima_aparicao_lcf(frames)) == 2

    def test_com_gap_retorna_ultimos(self):
        frames = [
            _lcf(offset_min=0),
            _lcf(offset_min=5),
            _lcf(offset_min=20),  # gap > 10min
            _lcf(offset_min=25),
        ]
        resultado = _pegar_ultima_aparicao_lcf(frames)
        assert len(resultado) == 2
        # Deve pegar os 2 ultimos (apos gap)
        assert resultado[0].lote.timestamp_frame == BASE_TS + timedelta(minutes=20)

    def test_um_frame(self):
        assert len(_pegar_ultima_aparicao_lcf([_lcf()])) == 1


# --- selecionar_frames_visuais ---

class TestSelecionarFramesVisuais:
    def test_menos_que_n_retorna_todos(self):
        frames = [_lcf(frame_name=f"f{i}.jpg") for i in range(3)]
        resultado = selecionar_frames_visuais(frames, n=4)
        assert len(resultado) == 3

    def test_exatamente_n(self):
        frames = [_lcf(frame_name=f"f{i}.jpg") for i in range(4)]
        resultado = selecionar_frames_visuais(frames, n=4)
        assert len(resultado) == 4

    def test_mais_que_n_seleciona_equidistantes(self):
        frames = [_lcf(frame_name=f"f{i}.jpg") for i in range(12)]
        resultado = selecionar_frames_visuais(frames, n=4)
        assert len(resultado) == 4
        # Deve ser equidistante: indices 0, 3, 6, 9
        assert resultado[0] == Path("/tmp/f0.jpg")
        assert resultado[1] == Path("/tmp/f3.jpg")


# --- _dedup_lotes_espelhados ---

class TestDedupLotesEspelhados:
    def _consolidado(
        self,
        numero: str,
        raca: str = "Nelore",
        sexo: str = "macho",
        qtd: int = 10,
        frames: int = 5,
        offset_min: float = 0,
    ) -> LoteConsolidado:
        return LoteConsolidado(
            lote_numero=numero,
            quantidade=qtd,
            raca=raca,
            sexo=sexo,
            preco_inicial=Decimal("1000"),
            preco_final=Decimal("2000"),
            local_cidade="Crixas",
            local_estado="GO",
            timestamp_inicio=BASE_TS + timedelta(minutes=offset_min),
            frames_analisados=frames,
            confianca_media=0.9,
        )

    def test_sem_espelhados(self):
        lotes = [self._consolidado("0001"), self._consolidado("0002")]
        resultado = _dedup_lotes_espelhados(lotes)
        assert len(resultado) == 2

    def test_detecta_espelhado_1000_0001(self):
        """1000 e 0001 sao espelhados se mesma raca/sexo/qtd/tempo."""
        lotes = [
            self._consolidado("1000", frames=3),
            self._consolidado("0001", frames=5),
        ]
        resultado = _dedup_lotes_espelhados(lotes)
        assert len(resultado) == 1
        assert resultado[0].lote_numero == "0001"  # mantem o com mais frames

    def test_nao_dedup_se_raca_diferente(self):
        """Numeros espelhados mas raca diferente = nao e dup."""
        lotes = [
            self._consolidado("1000", raca="Nelore"),
            self._consolidado("0001", raca="Angus"),
        ]
        resultado = _dedup_lotes_espelhados(lotes)
        assert len(resultado) == 2

    def test_nao_dedup_se_tempo_distante(self):
        """Numeros espelhados mas timestamps distantes (> 30min) = nao e dup."""
        lotes = [
            self._consolidado("1000", offset_min=0),
            self._consolidado("0001", offset_min=60),
        ]
        resultado = _dedup_lotes_espelhados(lotes)
        assert len(resultado) == 2


# --- consolidar_lotes (integracao) ---

class TestConsolidarLotes:
    def test_lote_simples(self, tmp_path):
        """Lote com 3 frames com preco crescente = arrematado."""
        frames = [
            _lcf("01", preco=1000, offset_min=0, frame_name="frame_000100.jpg"),
            _lcf("01", preco=1500, offset_min=1, frame_name="frame_000112.jpg"),
            _lcf("01", preco=2000, offset_min=2, frame_name="frame_000124.jpg"),
        ]
        resultado = consolidar_lotes(frames)
        assert len(resultado) == 1
        lote = resultado[0]
        assert lote.lote_numero == "01"
        assert lote.preco_inicial == Decimal("1000")
        assert lote.preco_final == Decimal("2000")
        assert lote.status == "arrematado"
        assert lote.frames_analisados == 3

    def test_lote_preco_zero_descartado_da_consolidacao(self, tmp_path):
        """Frames com preco 0 nao contam pra preco mas contam pra visuais."""
        frames = [
            _lcf("01", preco=0, offset_min=0, frame_name="frame_000001.jpg"),
            _lcf("01", preco=3000, offset_min=1, frame_name="frame_000013.jpg"),
            _lcf("01", preco=3500, offset_min=2, frame_name="frame_000025.jpg"),
        ]
        resultado = consolidar_lotes(frames)
        assert len(resultado) == 1
        assert resultado[0].preco_inicial == Decimal("3000")
        assert resultado[0].frames_analisados == 2  # so os com preco

    def test_lote_com_repescagem(self, tmp_path):
        """Lote que aparece 2x (gap >10min) = status repescagem."""
        frames = [
            _lcf("01", preco=1000, offset_min=0, frame_name="frame_000001.jpg"),
            _lcf("01", preco=1500, offset_min=5, frame_name="frame_000061.jpg"),
            # gap de 20 minutos
            _lcf("01", preco=2000, offset_min=25, frame_name="frame_000301.jpg"),
            _lcf("01", preco=2500, offset_min=30, frame_name="frame_000361.jpg"),
        ]
        resultado = consolidar_lotes(frames)
        assert len(resultado) == 1
        assert resultado[0].status == "repescagem"
        assert resultado[0].aparicoes == 2
        # Preco final deve ser da ultima aparicao
        assert resultado[0].preco_final == Decimal("2500")

    def test_lote_unico_baixa_confianca_descartado(self, tmp_path):
        """Lote com 1 frame e confianca < 90% descartado."""
        frames = [_lcf("01", preco=3000, confianca=0.7, frame_name="frame_000001.jpg")]
        resultado = consolidar_lotes(frames)
        assert len(resultado) == 0

    def test_lote_unico_alta_confianca_mantido(self, tmp_path):
        """Lote com 1 frame e confianca >= 90% mantido."""
        frames = [_lcf("01", preco=3000, confianca=0.95, frame_name="frame_000001.jpg")]
        resultado = consolidar_lotes(frames)
        assert len(resultado) == 1

    def test_multiplos_lotes(self, tmp_path):
        """Varios lotes diferentes consolidam separadamente."""
        frames = [
            _lcf("01", preco=1000, offset_min=0, frame_name="frame_000001.jpg"),
            _lcf("01", preco=2000, offset_min=5, frame_name="frame_000061.jpg"),
            _lcf("02", preco=3000, offset_min=10, frame_name="frame_000121.jpg"),
            _lcf("02", preco=4000, offset_min=15, frame_name="frame_000181.jpg"),
        ]
        resultado = consolidar_lotes(frames)
        assert len(resultado) == 2
        numeros = {l.lote_numero for l in resultado}
        assert numeros == {"01", "02"}
```

**Step 2: Run tests**

Run: `python -m pytest tests/test_processor.py -v`
Expected: All PASSED (testes contra funcoes existentes — devem passar).

Se algum falhar, e um bug real no processor.py que sera investigado.

**Step 3: Commit**

```bash
git add tests/test_processor.py
git commit -m "test: cobertura do processor.py (consolidacao, dedup, repescagem)"
```

---

## Task 4: React Query no dashboard

**Files:**
- Modify: `dashboard/package.json` (add @tanstack/react-query)
- Create: `dashboard/src/lib/query-provider.tsx`
- Modify: `dashboard/src/main.tsx`
- Modify: `dashboard/src/components/metricas-cards.tsx`
- Modify: `dashboard/src/components/lotes-table.tsx`
- Modify: `dashboard/src/components/tendencia-chart.tsx`
- Modify: `dashboard/src/components/paineis.tsx`
- Modify: `dashboard/src/components/filtro-bar.tsx`
- Modify: `dashboard/src/pages/leiloes.tsx`

**Step 1: Install React Query**

```bash
cd dashboard && npm install @tanstack/react-query
```

**Step 2: Create QueryProvider**

```tsx
// dashboard/src/lib/query-provider.tsx
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";

const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 30_000,
      retry: 1,
    },
  },
});

export function QueryProvider({ children }: { children: React.ReactNode }) {
  return (
    <QueryClientProvider client={queryClient}>
      {children}
    </QueryClientProvider>
  );
}
```

**Step 3: Wrap App with QueryProvider**

Em `dashboard/src/main.tsx`, importar e envolver:

```tsx
import { QueryProvider } from "@/lib/query-provider";

// No render:
<QueryProvider>
  <App />
</QueryProvider>
```

**Step 4: Commit setup**

```bash
git add dashboard/package.json dashboard/package-lock.json dashboard/src/lib/query-provider.tsx dashboard/src/main.tsx
git commit -m "chore: setup React Query no dashboard"
```

**Step 5: Migrar metricas-cards.tsx**

Substituir `useState` + `useEffect` por `useQuery`:

```tsx
import { useQuery } from "@tanstack/react-query";
// Remover: import { useEffect, useState } from "react";

export function MetricasCards({ filtros }: MetricasCardsProps) {
  const { data, isLoading, isError } = useQuery({
    queryKey: ["metricas", filtros],
    queryFn: () => api.metricas(filtros),
  });

  if (isLoading || !data) {
    return (
      <div className="grid grid-cols-2 gap-3">
        {[1, 2, 3, 4].map((i) => (
          <Card key={i} className="animate-pulse">
            <CardHeader className="pb-2"><div className="h-4 bg-muted rounded w-20" /></CardHeader>
            <CardContent><div className="h-8 bg-muted rounded w-32" /></CardContent>
          </Card>
        ))}
      </div>
    );
  }

  if (isError) {
    return (
      <div className="grid grid-cols-2 gap-3">
        <Card className="col-span-2">
          <CardContent className="py-6 text-center text-sm text-destructive">
            Erro ao carregar metricas
          </CardContent>
        </Card>
      </div>
    );
  }

  // ... resto do componente igual (usa `data` que ja existia)
```

**Step 6: Migrar lotes-table.tsx**

```tsx
import { useQuery } from "@tanstack/react-query";
// Remover useEffect do import

export function LotesTable({ filtros, onPlayVideo }: LotesTableProps) {
  const { data: lotes = [], isLoading: loading, isError } = useQuery({
    queryKey: ["lotes", filtros],
    queryFn: () => api.lotes(filtros),
  });
  const [expandido, setExpandido] = useState<number | null>(null);
  const [lightbox, setLightbox] = useState<{ paths: string[]; index: number; lote: string } | null>(null);

  // Remover o useEffect inteiro
  // Remover useState de lotes e loading

  // Adicionar tratamento de erro antes do return:
  // if (isError) → mensagem de erro

  // ... resto igual, ja usa `lotes` e `loading`
```

**Step 7: Migrar tendencia-chart.tsx**

```tsx
import { useQuery } from "@tanstack/react-query";

export function TendenciaChart({ filtros }: TendenciaChartProps) {
  const { data = [], isLoading } = useQuery({
    queryKey: ["tendencia", filtros],
    queryFn: () => api.tendencia(filtros),
  });

  // Remover useState e useEffect
  // ... resto igual, ja usa `data`
```

**Step 8: Migrar paineis.tsx**

Este componente faz 2 fetches. Usar 2 useQuery:

```tsx
import { useQuery } from "@tanstack/react-query";

export function Paineis({ filtros }: PaineisProps) {
  const { data: regioes = [] } = useQuery({
    queryKey: ["regioes", filtros],
    queryFn: () => api.regioes(filtros),
  });
  const { data: fazendas = [] } = useQuery({
    queryKey: ["fazendas", filtros],
    queryFn: () => api.fazendas(filtros),
  });

  // Remover useState e useEffect
  // ... resto igual
```

**Step 9: Migrar filtro-bar.tsx**

```tsx
import { useQuery } from "@tanstack/react-query";

export function FiltroBar({ ... }: FiltroBarProps) {
  const { data: opcoes } = useQuery({
    queryKey: ["filtros"],
    queryFn: () => api.filtros(),
    staleTime: 60_000, // opcoes mudam pouco
  });

  // Remover useState e useEffect
  // ... resto igual, ja usa `opcoes`
```

**Step 10: Migrar leiloes.tsx**

```tsx
import { useQuery } from "@tanstack/react-query";

export function LeiloesPage() {
  const { data: leiloes = [], isLoading: loading } = useQuery({
    queryKey: ["leiloes"],
    queryFn: () => api.leiloes(),
  });

  // Remover useState de leiloes e loading
  // Remover useEffect inteiro
  // Adicionar api.leiloes() na lib/api.ts se nao existir (ja existe: api.leiloes nao existe, leiloes.tsx usa fetch direto)
```

**Nota:** `leiloes.tsx` usa `fetch("http://localhost:8000/api/leiloes")` direto em vez de `api.leiloes()`. Precisa primeiro adicionar ao `api.ts`:

```typescript
// Em dashboard/src/lib/api.ts, adicionar ao objeto api:
  leiloes: () => fetchJson<{ id: number; titulo: string; canal: string; local_cidade: string | null; local_estado: string | null; total_lotes: number | null; processado_em: string | null; status: string }[]>("/api/leiloes"),
```

E mover a interface `Leilao` de `leiloes.tsx` pra `api.ts`.

**Step 11: Verify — iniciar dashboard e testar**

```bash
cd dashboard && npm run dev
```

Abrir http://localhost:5173 e verificar:
- Dashboard carrega com filtros
- Mudar filtro atualiza dados
- Pagina de leiloes carrega

**Step 12: Commit**

```bash
cd /Users/mority/Dev/Projects/leilao-inteligente
git add dashboard/src/
git commit -m "refactor: migrar dashboard para React Query"
```
